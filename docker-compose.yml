services:
  fastapi:
    build: .
    ports:
      - "8000:8000"

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: breast_cancer_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: